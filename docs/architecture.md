# Runna Architecture Notes

## Phase 1 baseline

- `mobile/` will host the Flutter app
- `backend/` hosts the FastAPI API
- `db` runs PostgreSQL with PostGIS
- future routing service will be added for route generation

## Immediate next implementation targets

1. user model and auth flows
2. emergency contact model
3. run tracking models
4. route model

